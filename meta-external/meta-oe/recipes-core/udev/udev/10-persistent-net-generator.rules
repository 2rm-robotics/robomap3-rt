#regle evitant le renomage des interfaces reseaux par udev (au cas ou la carte sd est mise dans une autre igep par exemple)
#on attribue un nom, et la regle 75-persistent-net-generator.rules sera passee
KERNEL=="wlan0", NAME="wlan0"
KERNEL=="eth0", NAME="eth0"
KERNEL=="ath0", NAME="wlan0"
